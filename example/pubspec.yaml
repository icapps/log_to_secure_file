name: example
description: Example flutter project consuming the library
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  log_to_secure_file: 
    path: ..

flutter:
  uses-material-design: true
